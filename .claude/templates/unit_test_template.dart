// Esqueleto de unit test do motor de jogo (Dart puro, sem flutter_test).
// Ver .claude/rules/testing.md e .claude/commands/generate-level.md
// (usado para provar que uma fase nova é solucionável dentro do maxBlocks).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('programa esperado leva o mascote ao alvo', () {
    // final level = exampleLevel; // ver .claude/templates/level_template.dart
    // final program = Program([...]); // sequência de Block que resolve a fase
    // final result = ProgramExecutor(level).run(program);
    // expect(result.isVictory, isTrue);
  });
}
