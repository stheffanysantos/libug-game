# Documentação — Debuga o Mascote

Índice da documentação viva em `.claude/docs/`. Estes documentos detalham o que `CLAUDE.md` resume.

| Documento | Conteúdo |
|---|---|
| `GETTING_STARTED.md` | Como rodar o projeto e os testes |
| `GAME_DESIGN.md` | Regras do jogo: grid, comandos, condições de vitória/derrota, estrelas |
| `ARCHITECTURE.md` | Camadas (models/game/screens/widgets) e fluxo de dados |
| `FOLDER_STRUCTURE.md` | Árvore de `lib/` explicada pasta a pasta |
| `NAVIGATION_FLOW.md` | As 5 telas e as transições entre elas |
| `AGENTS_WORKFLOW.md` | Como os subagentes de `.claude/agents/` colaboram |

Para regras obrigatórias (o que **deve** ser seguido, não só o que existe), ver `.claude/rules/`, não esta pasta — `docs/` é descritivo, `rules/` é normativo.
